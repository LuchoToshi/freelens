---
phase: 01-calculation-engine
plan: 03
type: execute
wave: 3
depends_on: ["01-calculation-engine/02"]
files_modified: []
autonomous: false
requirements: [CALC-01, CALC-02, CALC-03, CALC-04, CALC-05, CALC-06, CALC-07]
must_haves:
  truths:
    - "User has manually verified the calculator works end-to-end in a real browser"
  artifacts: []
  key_links: []
---

<objective>
Human verification gate for Phase 1. The calculator must be exercised in a real browser against the four scenarios that prove every CALC requirement is satisfied. No code changes — this plan only runs the dev server and waits for user sign-off.

Purpose: Catch any regression or visual issue automated checks can't see (slider thumb appearance, focus rings, dash spacing, reactive feel).
</objective>

<context>
@.planning/phases/01-calculation-engine/01-UI-SPEC.md
@.planning/phases/01-calculation-engine/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser smoke test of calculator</name>
  <read_first>
    - .planning/phases/01-calculation-engine/01-UI-SPEC.md
  </read_first>
  <what-built>
    Working calculator from plans 01 + 02: four inputs (balance, tax rate slider, monthly expenses, runway months), four-line breakdown, reactive output, over-reserved $0 state with explanatory copy.
  </what-built>
  <how-to-verify>
1. From `/Users/shariffshariff/Documents/Freelens`, run `npm run dev` and open the printed localhost URL.
2. **Initial state check:** Page loads instantly. Tax rate slider sits at 30%. Runway shows 3. Balance and Monthly expenses are empty. Breakdown shows four labeled rows: Balance, Tax reserve, Runway buffer, Safe to spend — each value is `—`. Safe to spend `—` is in the large display style (36px).
3. **Reactive happy path:** Type `10000` in balance. Confirm Balance row shows `$10,000`, Tax reserve shows `$3,000`, Runway buffer still `—` (no expenses yet), Safe to spend `—`.
4. Type `2000` in monthly expenses. Confirm: Runway buffer = `$6,000`, Safe to spend = `$1,000`, no over-reserved message.
5. **Slider reactivity:** Drag tax slider to 50. Display reads `50%`. Tax reserve = `$5,000`, Safe to spend = `$0`, over-reserved message appears: "Your tax reserve and runway buffer use up your full balance. Nothing left to spend freely." No red color, no alert styling — calm gray text.
6. Drag slider back to 15. Display reads `15%`. Over-reserved message disappears. Safe to spend = `$2,500`.
7. **Runway change:** Type `12` in runway months. Runway buffer recomputes to `$24,000`. Safe to spend clamps to `$0`, message reappears.
8. **Empty state recovery:** Clear the balance field entirely. All three dependent rows (Balance, Tax reserve, Safe to spend) revert to `—`. Runway buffer should still show `$24,000` (only depends on expenses + months).
9. **Slider track fill:** Drag slider across the range. Confirm the filled (dark) portion of the track grows/shrinks with the thumb position — not static.
10. **Focus ring:** Tab through inputs. Each focused input shows a 2px dark outline.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues for plan 04 gap closure.</resume-signal>
</task>

</tasks>

<verification>
User signed off on all 10 manual checks above.
</verification>

<success_criteria>
Phase 1 is shippable: every CALC-01..07 requirement verified in a real browser.
</success_criteria>

<output>
After completion, create `.planning/phases/01-calculation-engine/01-03-SUMMARY.md` recording any issues raised + resolution.
</output>
