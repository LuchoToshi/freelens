---
phase: 01-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/index.css
  - src/App.tsx
  - src/vite-env.d.ts
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `npm run build` exits 0"
    - "Running `npm run dev` serves an empty React app on localhost"
    - "Tailwind v4 utility classes apply at runtime"
  artifacts:
    - path: "package.json"
      provides: "Vite + React 19 + TS + Tailwind v4 dependencies"
      contains: "@tailwindcss/vite"
    - path: "vite.config.ts"
      provides: "Vite config with React + Tailwind v4 plugins"
      contains: "tailwindcss()"
    - path: "src/index.css"
      provides: "Tailwind v4 import"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugins array"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Scaffold the Freelens project: React 19 + Vite 6 + TypeScript 5 + Tailwind CSS v4. Strip Vite default styles/assets so Phase 1 plan 02 can drop in the Calculator without conflicts. No app code yet — just the foundation that builds clean.

Purpose: Establish the build/dev infrastructure required by all subsequent work.
Output: Working Vite dev server, clean `src/` ready for Calculator component.
</objective>

<context>
@.planning/phases/01-calculation-engine/01-CONTEXT.md
@.planning/phases/01-calculation-engine/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React-TS project + install Tailwind v4</name>
  <read_first>
    - CLAUDE.md (project root)
    - .planning/phases/01-calculation-engine/01-RESEARCH.md (Standard Stack section, Pitfall 5 + 6)
  </read_first>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/vite-env.d.ts, .gitignore</files>
  <action>
1. From repo root `/Users/shariffshariff/Documents/Freelens`, run: `npm create vite@latest . -- --template react-ts` (when prompted about non-empty directory, choose "Ignore files and continue").
2. Run: `npm install`
3. Run: `npm install tailwindcss@^4 @tailwindcss/vite@^4`
4. Overwrite `vite.config.ts` to:
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```
5. Verify `tsconfig.app.json` has `"strict": true` (Vite template default — leave as-is).
6. Run: `npm run build` — must exit 0.
  </action>
  <verify>
    <automated>cd /Users/shariffshariff/Documents/Freelens && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` exists and contains the string `"@tailwindcss/vite"`
    - File `package.json` contains `"react": "^19` (or higher)
    - File `vite.config.ts` contains `tailwindcss()` inside the plugins array
    - File `vite.config.ts` contains `import tailwindcss from '@tailwindcss/vite'`
    - `npm run build` exits 0
    - Directory `node_modules/@tailwindcss/vite` exists
  </acceptance_criteria>
  <done>Vite scaffold installed, Tailwind v4 plugin wired into vite.config.ts, `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Vite defaults, wire Tailwind v4 import, create blank App shell</name>
  <read_first>
    - src/App.tsx (current Vite default)
    - src/index.css (current Vite default)
    - src/main.tsx
    - .planning/phases/01-calculation-engine/01-RESEARCH.md (Pitfall 5 + 6, index.css example)
  </read_first>
  <files>src/App.tsx, src/index.css, src/App.css (delete), src/assets/react.svg (delete)</files>
  <action>
1. Delete files: `src/App.css`, `src/assets/react.svg`. If `public/vite.svg` exists, leave it (referenced by index.html — optional).
2. Overwrite `src/index.css` with EXACTLY:
```css
@import "tailwindcss";
```
3. Overwrite `src/App.tsx` with EXACTLY:
```tsx
export default function App() {
  return (
    <div className="min-h-screen bg-white">
      {/* Calculator component lands here in plan 02 */}
    </div>
  );
}
```
4. Open `src/main.tsx` and remove any `import './App.css'` line if present. Ensure it still imports `./index.css`.
5. Run `npm run build` — must exit 0.
  </action>
  <verify>
    <automated>cd /Users/shariffshariff/Documents/Freelens && npm run build && test ! -f src/App.css && grep -q '@import "tailwindcss"' src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - File `src/App.css` does NOT exist
    - File `src/index.css` contains exactly the line `@import "tailwindcss";`
    - File `src/App.tsx` does NOT contain the string `import './App.css'`
    - File `src/App.tsx` does NOT contain `logo` or `count is`
    - File `src/main.tsx` contains `import './index.css'`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Vite default boilerplate removed, Tailwind v4 imported, App.tsx is a clean shell ready for Calculator mount.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `npm run dev` starts and serves a blank white page on localhost
- No leftover Vite logo/counter UI
</verification>

<success_criteria>
Foundation ready: Vite + React 19 + TS + Tailwind v4 builds clean, App.tsx is a blank container, no default cruft.
</success_criteria>

<output>
After completion, create `.planning/phases/01-calculation-engine/01-01-SUMMARY.md`
</output>
