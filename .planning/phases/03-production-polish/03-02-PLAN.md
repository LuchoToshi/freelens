---
phase: 03-production-polish
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "Invalid balance (-100) shows 'Vul een positief saldo in' on blur; correcting clears it"
    - "Runway = 0 shows 'Voer minimaal 1 maand buffer in' on blur; correcting clears it"
    - "Tax slider at 0% shows calm soft warning; calculation still displays a number"
    - "On mobile viewport (375px), runway input is full-width matching balance and monthly expenses"
    - "Hard refresh with cache disabled shows zero XHR/fetch network requests"
    - "Entering 3200 in balance displays as €3.200 in the Saldo breakdown row"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the full Production Polish behavior in a real browser and mobile viewport. This is the phase's final gate — no code changes, just confirming UX-01 through UX-04 are observably true.

Purpose: UX behavior cannot be unit tested per project constraints (CLAUDE.md: no test suite for validation prototype). Manual verification is the correct mechanism.
Output: User approval (or list of issues to fix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-production-polish/03-CONTEXT.md
@.planning/phases/03-production-polish/03-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Production Polish in browser and mobile viewport</name>
  <read_first>
    - .planning/phases/03-production-polish/03-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/03-production-polish/03-CONTEXT.md (D-04, D-05, D-06 exact copy)
  </read_first>
  <what-built>
    Calculator.tsx with blur-triggered validation (balance, runway), soft warning for 0% tax, full-width runway input, existing nl-NL currency formatting. All changes in `src/components/Calculator.tsx`; `src/lib/format.ts` unchanged (D-12/D-13).
  </what-built>
  <action>
Claude: start the dev server if not running: `npm run dev` and share the URL. Do not modify code in this task. Pause and wait for the user to report results per the checklist below.
  </action>
  <how-to-verify>
**Desktop browser (http://localhost:5173):**

1. **UX-01 negative balance:** Type `-100` in Huidig saldo, press Tab. Expect red text below input: `Vul een positief saldo in`. Clear field, type `500`, Tab. Error disappears.
2. **UX-01 zero runway:** Clear Buffer (maanden) and type `0`, Tab. Expect red text below: `Voer minimaal 1 maand buffer in`. Type `3`, Tab. Error disappears.
3. **UX-01 0% tax soft warning:** Drag tax slider to 0%. Expect calm muted text (not red) below slider: `Controleer je belastingtarief — 0% is ongebruikelijk voor ZZP'ers.`. Confirm the Veilig te besteden number still updates (calculation not blocked).
4. **UX-01 no dialogs:** At no point should an alert/toast/modal appear.
5. **UX-04 format:** Enter `3200` in balance, `1000` in monthly expenses. Confirm the Saldo row displays `€3.200` (Dutch notation, period as thousands separator).

**Mobile verification (DevTools responsive, iPhone 12 Pro ~390px or real device):**

6. **UX-02 runway full-width:** Buffer (maanden) input spans the same width as Huidig saldo and Maandelijkse vaste lasten. No narrow box.
7. **UX-02 tappability:** Tap each input on real device (or simulate). Native keyboard opens, input accepts values.

**Network (DevTools Network tab, Disable cache checked):**

8. **UX-03 instant load:** Hard refresh. Filter XHR/Fetch — should be 0 requests. Only document + JS/CSS assets. No spinner, no login screen.

Reply with `approved` if all 8 checks pass, or list the failing check numbers with observed behavior.
  </how-to-verify>
  <acceptance_criteria>
    - User replies `approved` OR reports specific failures that must be resolved before phase completion
    - All 8 checks above produce the expected outcome
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which check failed and what you observed</resume-signal>
  <done>User has approved all Production Polish behaviors or filed specific issues for revision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — verification-only plan, no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | N/A | verification checkpoint | accept | No code changes; threats covered by Plan 01 threat model. |
</threat_model>

<verification>
User approval via checkpoint response.
</verification>

<success_criteria>
All 8 manual verification checks pass in browser and mobile viewport.
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-polish/03-02-SUMMARY.md`
</output>
